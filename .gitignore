# --- R session / project artifacts ---
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj.bak

# --- knitr / rmarkdown render output ---
*_cache/
*_files/
notebooks/*_cache/
notebooks/*_files/

# Rendered HTML from the lessons IS committed so participants can read the
# lessons offline in a browser without knitting (self-contained files).

# --- files produced by RUNNING the lessons / capstone ---
notebooks/*.csv
figures/*.png
figures/*.pdf

# --- OS junk ---
.DS_Store
Thumbs.db
desktop.ini

# NOTE: the data/ CSVs ARE committed on purpose — the HITChip Atlas dataset is
# released under CC0, which permits redistribution. See data/SOURCE.md.
